output "powershell" {
    description = "The PowerShell-escaped string."
    value = local.escaped_powershell
}
output "unix" {
    description = "The Unix-escaped string."
    value = local.escaped_unix
}
